plugins {
    id 'fabric-loom' version '1.6.12'
    id 'maven-publish'
}

repositories {
    // Можно убрать этот блок и оставить репозитории в settings.gradle, но так тоже ок.
    maven { url 'https://libraries.minecraft.net/' } // Mojang libs
    maven { url 'https://maven.fabricmc.net/' }      // Fabric
    mavenCentral()
}

dependencies {
    minecraft "com.mojang:minecraft:1.20.1"

    // В Loom 1.6 НЕЛЬЗЯ использовать loom.yarn(...).
    // Правильно — обычной координатой:
    mappings "net.fabricmc:yarn:1.20.1+build.10:v2"

    // Загрузчик
    modImplementation "net.fabricmc:fabric-loader:0.14.22"

    // ТОЛЬКО монолитный Fabric API. Никаких подмодулей!
    modImplementation "net.fabricmc.fabric-api:fabric-api:0.92.3+1.20.1"
}

java {
    toolchain { languageVersion = JavaLanguageVersion.of(17) }
}
